@model IEnumerable<ProductListDto>

@{
    ViewData["Title"] = "Index";
}

<!DOCTYPE html>
<html lang="en">
<head>
    <title>Bootstrap Example</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
</head>
<body>
    <button type="button" class="btn btn-primary float-end" data-bs-toggle="modal" data-bs-target="#createEditModal" data-url="@Url.Action("Create")" style="margin-left:5%;">
        <i class="fa fa-plus" aria-hidden="true"></i>
    </button>
    <div class="container mt-3">
        <h2>Product List</h2>
        <table class="table table-bordered">
            <thead>
                <tr>
                    <th>@Html.DisplayNameFor(model => model.Id)</th>
                    <th>@Html.DisplayNameFor(model => model.Title)</th>
                    <th>@Html.DisplayNameFor(model => model.Description)</th>
                    <th>Category</th>
                    <th>Brand</th>
                    <th>Color</th>
                    <th>@Html.DisplayNameFor(model => model.Price)</th>
                    <th>Image</th>
                    <th>@Html.DisplayNameFor(model => model.InStock)</th>
                    <th>Active</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                @*              @foreach (var item in Model)
                {*@
                <tr>
                    @*     <td>@item.Title</td>
                    <td>@item.Description</td>
                    <td>@item.Description</td>
                    <td>@item.CategoryId</td>
                    <td>@item.BrandId</td>
                    <td>@item.ColorId</td>
                    <td>@item.Price</td>
                    <td>@item.ImagePath</td>
                    <td>@item.IsActive</td>
                    @*      <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                    <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                    <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>*@
                    @* <td>
                    <a asp-page="./Edit" asp-route-id="@item.Id">
                    <i class="fa fa-eye btn btn-info" aria-hidden="true"></i>
                    </a>
                    <a asp-page="./Edit" asp-route-id="@item.Id">
                    <i class="fa fa-pencil-square-o btn btn-success" aria-hidden="true"></i>
                    </a>
                    <a asp-page="./Edit" asp-route-id="@item.Id">
                    <i class="fa fa-trash btn btn-danger" aria-hidden="true"></i>
                    </a>
                    </td>*@
                    <td>Id</td>
                    <td>Title</td>
                    <td>Description</td>
                    <td>Description</td>
                    <td>CategoryId</td>
                    <td>BrandId</td>
                    <td>ColorId</td>
                    <td>Price</td>
                    <td>ImagePath</td>
                    <td>IsActive</td>
                    <td>
                        @*<a asp-page="" asp-route-id="">*@
                            <i class="btn btn-info fa fa-eye" aria-hidden="true"></i>
                        @*</a> &nbsp;*@
                       @*<a asp-page="" asp-route-id="">*@
                            <i class="fa fa-pencil-square-o btn btn-success" aria-hidden="true"></i>
                            
                         <a asp-page="" asp-route-id="">
                            <i class="fa fa-trash btn btn-danger" aria-hidden="true"></i>
                        </a>
                    </td>
                </tr>
                @*}*@
            </tbody>
        </table>
    </div>
</body>
</html>

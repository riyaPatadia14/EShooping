@model PaginatedList<ProductListDto>

@{
    ViewData["Title"] = "Index";
}

    <div id="ProductPlaceHolderHere"></div>
    <div class="d-flex justify-content-center" style="padding-left:68%">
    <button type="button" class="btn btn-primary"  data-toggle="ajax-modal" data-target="#btnDetailProductModal" id="btnDetailProductModal" data-url="@Url.Action($"Create")">
       @* <i class="fa fa-plus" aria-hidden="true"></i>*@ Create
    </button> 
    </div>
    @*   <button type="button" class="btn btn-primary float-end" data-bs-toggle="modal" id="btnCreateProductModal" style="margin-left:5%;">
    <i class="fa fa-plus" aria-hidden="true"></i>
    </button>*@
    <div class="container mt-3">
        <h2>Product List</h2>
        <table class="table table-bordered">
            <thead>
                <tr>
                    <th>Title</th>
                    <th>Description</th>
                    <th>Category</th>
                    <th>Brand</th>
                    <th>Color</th>
                    <th>Price</th>
                    <th>Image</th>
                    <th>InStock</th>
                    <th>Active</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.Title</td>
                        <td>@item.Description</td>
                        <td>@item.CategoryName</td>
                        <td>@item.BrandName</td>
                        <td>@item.ColorName</td>
                        <td>@item.Price</td>
                        <td><img src="~/img/category-img/@item.ImagePath" style="width:100px; height:100px" /></td>
                        <td>@item.InStock</td>
                        <td>@item.IsActive</td>
                        <td>
                            <button type="button" class="btn btn-info" data-bs-toggle="modal" data-toggle="ajax-modal" data-target="#btnDetailModal" id="btnDetailProductModal" data-url="@Url.Action($"Details/{item.Id}")">
                                Details
                            </button>
                            <button type="button" class="btn btn-success" data-bs-toggle="modal" data-toggle="ajax-modal" data-target="#editCategory" id="btnEditProductModal" data-url="@Url.Action($"Edit/{item.Id}")">
                                Edit
                            </button>
                            <button type="button" class="btn btn-danger" data-bs-toggle="modal" data-toggle="ajax-modal" data-target="#deleteCategory" id="btnDeleteProductModal" data-url="@Url.Action($"Delete/{item.Id}")">
                                Delete
                            </button>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>

    @{
        var prevDisabled = !Model.HasPreviousPage ? "disabled" : "";
        var nextDisabled = !Model.HasNextPage ? "disabled" : "";
    }

    <a asp-action="Index"
       asp-route-sortOrder="@ViewData["CurrentSort"]"
       asp-route-pageNumber="@(Model.PageIndex - 1)"
       asp-route-currentFilter="@ViewData["CurrentFilter"]"
       class="btn btn-default @prevDisabled">
        Previous
    </a>
    <a asp-action="Index"
       asp-route-sortOrder="@ViewData["CurrentSort"]"
       asp-route-pageNumber="@(Model.PageIndex + 1)"
       asp-route-currentFilter="@ViewData["CurrentFilter"]"
       class="btn btn-default @nextDisabled">
        Next
    </a>
    @*Create Modal Start*@
@*<div class="modal fade" id="createProductModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLabel">Add New Category</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                @Html.Partial("_Create", new ProductAddDto())
            </div>
            <div class="modal-footer">
            </div>
        </div>
    </div>
</div>*@
    @*Create Modal End*@
     @section scripts{
        <script src="~/js/product/model.js"></script>
    }

